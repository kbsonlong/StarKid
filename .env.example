# =============================================================================
# 环境变量配置模板
# =============================================================================
# 复制此文件为 .env 并填入实际的配置值
# 注意：.env 文件不应提交到版本控制系统

# =============================================================================
# Cloudflare Workers API 配置 - 前端环境变量
# =============================================================================
# 注意：此文件包含前端应用所需的公开环境变量
# 这些变量会被打包到前端代码中，因此不应包含敏感信息

# Cloudflare Workers API 基础 URL
# 格式：https://your-worker.your-subdomain.workers.dev
VITE_API_BASE_URL=https://your-worker.your-subdomain.workers.dev

# =============================================================================
# 配置说明
# =============================================================================
# 1. 部署 Cloudflare Workers 后端
# 2. 获取 Workers 的部署 URL
# 3. 将 URL 设置为 VITE_API_BASE_URL 的值
#
# ⚠️  安全提醒：
# - API 认证通过 JWT Token 管理
# - 所有敏感操作需要有效的认证 Token
# - 数据访问权限通过后端中间件控制
# - 文件上传使用 Cloudflare R2 存储
#
# ✅ 当前配置安全性：
# - 仅包含前端必需的公开变量
# - 所有敏感操作通过 JWT 认证保护
# - 用户权限通过后端中间件管理
# - 数据存储在 Cloudflare D1 数据库